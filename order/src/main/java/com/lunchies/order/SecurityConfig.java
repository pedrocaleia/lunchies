package com.lunchies.order;

//@Configuration
//@EnableWebSecurity
public class SecurityConfig {
	
	/*@Bean
	public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
		/*http.mvcMatcher("/articles/**").authorizeRequests().mvcMatchers("/articles/**")
				.access("hasAuthority('SCOPE_articles.read')").and().oauth2ResourceServer().jwt();
		return http.build();*/
		
		/*http.authorizeHttpRequests((auth) -> auth.requestMatchers("/product/**").hasRole("USER").anyRequest().authenticated());
		return http.build();
	}*/

}
